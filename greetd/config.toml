[terminal]
# The VT to run the greeter on. Can be "next", "current" or a number
# designating the VT.
vt = 7

# The default session, also known as the greeter.
[default_session]
#command = "cage -s -m last -d regreet"
command = "niri -c /etc/greetd/niri-greetd.kdl"
user = "greetd"
